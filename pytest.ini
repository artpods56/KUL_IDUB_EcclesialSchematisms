[pytest]
log_cli = true
log_cli_level = INFO
log_cli_format = %(levelname)s %(name)s: %(message)s
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    single_api_owner: exercise the exclusive one-API-owner startup lease
