# Server-side only. Required when a graph uses the Mistral OCR node.
MISTRAL_API_KEY=

# API runtime
NOTARIUS_PROTOTYPE_WORKSPACE=.notarius-artifacts/prototype-workbench
NOTARIUS_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Compiled into the browser bundle.
NEXT_PUBLIC_NOTARIUS_API_URL=http://localhost:8000
