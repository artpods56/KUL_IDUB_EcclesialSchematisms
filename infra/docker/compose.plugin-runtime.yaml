services:
  api:
    environment:
      GRAFY_PLUGIN_RUNTIME_ENABLED: "true"
      GRAFY_PLUGIN_RUNTIME_PROFILE: ${GRAFY_PLUGIN_RUNTIME_PROFILE:-python-uv}
      GRAFY_PLUGIN_DOCKER_BINARY: /usr/local/bin/docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
